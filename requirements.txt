# Core pipeline
anthropic>=0.40.0
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=0.1.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# PDF parsing — primary
# Requires ~2-3GB RAM. Run on a local machine, not a constrained container.
# Install: pip install docling
docling>=2.0.0

# PDF parsing — fallback (lightweight, used when Docling unavailable)
pymupdf4llm>=0.0.17
pymupdf>=1.24.0

# API (optional)
fastapi>=0.110.0
uvicorn>=0.29.0

# CLI display
rich>=13.0.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
